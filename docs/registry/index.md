# CrossRegistry

The `CrossRegistry` is the most convenient access to the data on the Cross Platform.
It allows to connect to the platform given your username and password and then 
to work with your data. To access the data, you only need to now of the respective
contract and the registry fetches the data for you. It allows you to aggregate 
and label your data based on the references given in contract.