"""Faithful pydantic models of external data standards.

Internal module: these types are received by consumers (e.g. as the schema of a
released data resource) but are not part of the public API and are not re-exported
from `crosscontract`.
"""
